spring:
  application:
    name: sync-service
  webflux:
    base-path: /api

server:
  port: 8081
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.nightswatch.syncservice: DEBUG
