# Leave blank to use Vite proxy (recommended for local dev with backend on :8082)
VITE_API_URL=
VITE_WS_URL=/api/ws-sync

# For production / standalone deploys, set these explicitly:
# VITE_API_URL=https://api.yourdomain.com
# VITE_WS_URL=https://api.yourdomain.com/api/ws-sync
